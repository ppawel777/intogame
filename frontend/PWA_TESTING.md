# Чеклист проверки PWA

## 1. Проверка Service Worker

### В Chrome/Edge DevTools:

1. Открой приложение: `http://localhost:5173`
2. Открой DevTools (F12)
3. Перейди в **Application** → **Service Workers**
4. Проверь:
   - ✅ Service Worker зарегистрирован
   - ✅ Статус: `activated and is running`
   - ✅ Scope: `/`
   - ✅ Источник: `http://localhost:5173`

### В консоли браузера:

```javascript
// Проверка регистрации
navigator.serviceWorker.getRegistrations().then((regs) => {
   console.log('Service Workers:', regs.length)
   regs.forEach((reg) => console.log('SW:', reg.scope, reg.active?.scriptURL))
})

// Проверка готовности
navigator.serviceWorker.ready.then((reg) => {
   console.log('SW готов:', reg)
})
```

**Ожидаемый результат:** Service Worker должен быть зарегистрирован и активен.

---

## 2. Проверка Manifest

### В Chrome/Edge DevTools:

1. **Application** → **Manifest**
2. Проверь:
   - ✅ Name: "В игру"
   - ✅ Short name: "В игру"
   - ✅ Icons: должны быть видны иконки (192x192 и 512x512)
   - ✅ Start URL: `/`
   - ✅ Display: `standalone`
   - ✅ Theme color: `#61b556`

### В консоли:

```javascript
// Проверка manifest
fetch('/manifest.webmanifest')
   .then((r) => r.json())
   .then((manifest) => console.log('Manifest:', manifest))
```

**Ожидаемый результат:** Manifest должен загружаться и содержать все необходимые поля.

---

## 3. Проверка иконок

### Проверка файлов:

1. Убедись, что файлы существуют в `public/`:
   - `icon-192x192.png`
   - `icon-512x512.png`
   - `favicon.ico`

2. Проверь доступность:
   - `http://localhost:5173/icon-192x192.png` - должен открываться
   - `http://localhost:5173/icon-512x512.png` - должен открываться

**Ожидаемый результат:** Все иконки должны быть доступны.

---

## 4. Проверка Push-уведомлений

### Шаг 1: Включение уведомлений

1. Войди в приложение (авторизуйся)
2. Перейди в **Профиль**
3. Найди переключатель "Push-уведомления"
4. Включи его
5. Разреши уведомления в браузере (появится запрос)

### Шаг 2: Проверка подписки

В консоли:

```javascript
// Проверка разрешения
console.log('Notification permission:', Notification.permission)

// Проверка подписки
navigator.serviceWorker.ready.then((reg) => {
   reg.pushManager.getSubscription().then((sub) => {
      if (sub) {
         console.log('Подписка активна:', sub.endpoint)
         console.log('Keys:', {
            p256dh: btoa(String.fromCharCode(...new Uint8Array(sub.getKey('p256dh')))),
            auth: btoa(String.fromCharCode(...new Uint8Array(sub.getKey('auth')))),
         })
      } else {
         console.log('Подписка не найдена')
      }
   })
})
```

### Шаг 3: Проверка в БД

Проверь, что подписка сохранена в Supabase:

```sql
SELECT * FROM push_subscriptions
WHERE user_id = <твой_user_id>;
```

**Ожидаемый результат:**

- Разрешение: `granted`
- Подписка создана и сохранена в БД

---

## 5. Тестовая отправка уведомления

### Через API:

```bash
curl -X POST http://localhost:3000/api/push/send \
  -H "Content-Type: application/json" \
  -d '{
    "userIds": [твой_user_id],
    "title": "Тестовое уведомление",
    "body": "Если ты видишь это, push работает!",
    "url": "/",
    "data": {"test": true}
  }'
```

Или через консоль браузера:

```javascript
fetch('http://localhost:3000/api/push/send', {
   method: 'POST',
   headers: { 'Content-Type': 'application/json' },
   body: JSON.stringify({
      userIds: [твой_user_id],
      title: 'Тестовое уведомление',
      body: 'Если ты видишь это, push работает!',
      url: '/',
      data: { test: true },
   }),
})
   .then((r) => r.json())
   .then(console.log)
```

**Ожидаемый результат:** Уведомление должно появиться в браузере.

---

## 6. Проверка установки PWA

### На Desktop (Chrome/Edge):

1. В адресной строке появится иконка установки (➕)
2. Или: меню браузера → "Установить В игру"
3. После установки приложение откроется в отдельном окне
4. Проверь:
   - ✅ Иконка на рабочем столе/в меню
   - ✅ Открывается в отдельном окне (без адресной строки)
   - ✅ Работает как обычное приложение

### На Mobile (Android Chrome):

1. Открой приложение в браузере
2. Меню браузера → "Добавить на главный экран"
3. Подтверди установку
4. Проверь:
   - ✅ Иконка на главном экране
   - ✅ Открывается как приложение (без браузера)

**Ожидаемый результат:** PWA можно установить и запускать как приложение.

---

## 7. Проверка офлайн-режима

### Шаг 1: Кэширование

1. Открой приложение
2. Открой DevTools → **Application** → **Cache Storage**
3. Проверь наличие кэшей:
   - `intogame-v1` или подобный
   - `workbox-precache-*`

### Шаг 2: Офлайн-тест

1. Открой DevTools → **Network**
2. Включи **Offline** (галочка)
3. Обнови страницу (F5)
4. Проверь:
   - ✅ Приложение должно загрузиться
   - ✅ Основные страницы доступны
   - ✅ Статические ресурсы загружаются из кэша

**Ожидаемый результат:** Приложение работает в офлайн-режиме.

---

## 8. Проверка обновлений Service Worker

### Тест обновления:

1. Измени что-то в коде (например, текст на странице)
2. Пересобери: `npm run build` (или перезапусти dev сервер)
3. Обнови страницу
4. В DevTools → **Application** → **Service Workers**:
   - ✅ Должен появиться новый Service Worker в статусе "waiting"
   - ✅ После обновления страницы старый заменится новым

**Ожидаемый результат:** Service Worker обновляется автоматически.

---

## 9. Проверка Lighthouse (опционально)

### Запуск аудита:

1. Открой DevTools → **Lighthouse**
2. Выбери:
   - ✅ Progressive Web App
   - ✅ Performance (опционально)
3. Нажми "Analyze page load"
4. Проверь результаты:
   - ✅ PWA score должен быть > 90
   - ✅ Все чекбоксы должны быть пройдены

**Ожидаемый результат:** Высокий PWA score, все проверки пройдены.

---

## Быстрая проверка через консоль

Выполни этот скрипт в консоли браузера для быстрой проверки:

```javascript
;(async () => {
   console.log('=== PWA Check ===')

   // 1. Service Worker
   const swCheck = await navigator.serviceWorker.getRegistrations()
   console.log('✅ Service Worker:', swCheck.length > 0 ? 'Зарегистрирован' : '❌ Не найден')

   // 2. Manifest
   try {
      const manifest = await fetch('/manifest.webmanifest').then((r) => r.json())
      console.log('✅ Manifest:', manifest.name || '❌ Не найден')
   } catch {
      console.log('❌ Manifest: Ошибка загрузки')
   }

   // 3. Notifications
   console.log('✅ Notifications:', Notification.permission)

   // 4. Push subscription
   const reg = await navigator.serviceWorker.ready
   const sub = await reg.pushManager.getSubscription()
   console.log('✅ Push subscription:', sub ? 'Активна' : '❌ Не найдена')

   // 5. Install prompt
   console.log('✅ Installable:', 'beforeinstallprompt' in window ? 'Да' : 'Нет')

   console.log('=== End Check ===')
})()
```

---

## Типичные проблемы и решения

### Service Worker не регистрируется

**Причина:** Проблемы с HTTPS/HTTP или ошибки в коде
**Решение:**

- Убедись, что используешь `http://localhost:5173` (не https)
- Проверь консоль на ошибки
- Проверь, что файлы в `dev-dist/` генерируются

### Push-уведомления не работают

**Причина:** VAPID keys не настроены или подписка не сохранена
**Решение:**

- Проверь, что VAPID keys настроены на бекенде
- Проверь, что подписка сохранена в БД
- Проверь разрешение на уведомления в браузере

### Иконки не отображаются

**Причина:** Файлы отсутствуют или неправильные пути
**Решение:**

- Убедись, что файлы в `public/`
- Проверь пути в `vite.config.ts`
- Проверь доступность файлов по URL

### PWA не устанавливается

**Причина:** Не все требования PWA выполнены
**Решение:**

- Проверь manifest
- Убедись, что есть иконки
- Проверь, что Service Worker работает
- Используй HTTPS в production

---

## Готово!

Если все проверки пройдены, PWA настроен корректно и готов к использованию! 🎉
